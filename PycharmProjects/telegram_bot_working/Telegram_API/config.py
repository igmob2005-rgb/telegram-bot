import os
import hashlib
from pathlib import Path # Используем современную библиотеку для работы с путями

# ============================
# 🔐 Секреты (Secrets)
# Эти данные никогда не должны быть хардкодированы в коде!
# Если запускаете локально, то можете оставить дефолтные значения.
# ============================

# Токен Бота: Получаем из окружения или используем заглушку для разработки.
BOT_TOKEN = os.getenv("BOT_TOKEN")
# ВАЖНО: Если вы убрали дефолт, бот упадет при запуске без BOT_TOKEN. Это хорошо!

# Пароль Администратора и его хеш (Осторожно с этой логикой!)
ADMIN_PASSWORD = os.getenv("ADMIN_PASSWORD")
ADMIN_PASSWORD_HASH = os.getenv("ADMIN_PASSWORD_HASH")


# ============================
# <0xF0><0x9F><0x97><0x84>️ Пути к данным (Database Paths)
# ============================

# Используем Path для более чистого и надежного кроссплатформенного пути.
# Убедитесь, что эта папка существует при запуске бота!
BASE_DIR = Path(__file__).resolve().parent

# Определяем путь к БД в папке 'database' (Рекомендуемый подход)
DB_PATH = BASE_DIR / "database" / "bot_data.db"


# ============================
# 💡 Проверка и валидация конфигурации
# Эта функция — лучший способ убедиться, что бот запустится только с правильными данными!
# ============================

def validate_config():
    """Проверяет критически важные переменные окружения перед запуском."""
    if not BOT_TOKEN:
        raise ValueError("FATAL ERROR: BOT_TOKEN не установлен. Установите его через переменную окружения.")
    print("✅ Конфигурация успешно загружена.")

# Вызываем проверку при импорте, чтобы сразу знать, готовы ли мы к запуску.
try:
    validate_config()
except ValueError as e:
    print(f"🚨 КОНФИГУРАЦИЯ НЕЗАВЕРШЕНА: {e}")
    # В реальном боте здесь можно завершить работу программы (exit())